type Query {
	info: String!
	posts: [Post!]!
}

type Mutation {
	# addPost(): Post!

	deletePost(id: String!): Post!
	
	# updatePost(): Post!
}

type Post {
    id: ID!
		# coachId: ID!
    price: Int!
    position: String!
	description: String!
    # ratingsId: ID!
    # industry: Industry!
	# tags: String!
}

type Industry {
	id: ID!
	name: String!
}
