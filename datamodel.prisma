type User {
  id: ID! @id
  name: String!
}

type Post {
  id: ID! @id
  price: Int!
  position: String!
  industry: Industry!
  description: String!
  # coachId: ID!
  # ratingsId: ID!
}

type Industry {
	id: ID! @id
	name: String! @unique
  posts: [Post]!
}

