type User {
  id: ID! @id
  name: String!
}

type Post {
  id: ID! @id
  # coachId: ID!
  price: Int!
  position: String!
  # ratingsId: ID!
  # industry: Industry!
  description: String!
}

type Industry {
	id: ID! @id
	name: String!
}